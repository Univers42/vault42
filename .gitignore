# Rust build output
/target
**/*.rs.bk
*.pdb
fuzz/target
fuzz/corpus

# Local key material & secrets must NEVER be committed (radioactive — see THREAT-MODEL.md)
# NOTE: the runtime keystore is `*.v42` below — do NOT use a broad `keystore*` here, it
# silently swallows the `keystore.rs` / `keystore_io.rs` SOURCE files (it once did).
*.v42
.env
.env.local
*.seed
*.key
!deploy/**/*.key.example

# OS / editor noise
.DS_Store

# Cargo.lock IS committed (this workspace ships binaries — reproducible builds).

# operator-only local secrets (never committed)
*.local.txt
